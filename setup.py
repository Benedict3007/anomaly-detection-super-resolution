from setuptools import setup, find_packages

setup(
    name='anomaly-detection-thesis',
    version='0.1.0',
    description='anomaly detection using Transformer-based super-resolution models',
    author='Benedict Druegh',
    packages=find_packages(),
)
